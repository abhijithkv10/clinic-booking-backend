# Clinic Booking Backend — Node.js with NGINX

This repository contains the backend API and server configuration used in the clinic booking system.

## Stack

- Node.js backend API
- MongoDB database
- NGINX reverse proxy with SSL
- CORS enabled for frontend restriction

## Running the App

```bash
npm install
node app.js
